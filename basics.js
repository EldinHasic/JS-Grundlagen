
function fullName(name, surname) { // Vor und Nachname mit Abstand schreiben
    let value = (name.concat(" ", surname));
    return value;
}

console.log(fullName("Eldin","Hasic"));
// Task 1 done

function capitalizeLetters(capstr) { // Strings GROSSSCHREIBEN
    let value = (capstr.toUpperCase());
    return value;
} 

console.log(capitalizeLetters("baNaNe"));
// Task 2 done